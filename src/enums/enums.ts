export const ETab = {
  INDEX: 1,
};

/**
 * DON'T CHANGE the ID value below ! Is not an dynamic value.
 * Because we need it
 * for access to property of one random cocktail data.
 */
export const ECocktail = {
  INDEX: 0,
};
